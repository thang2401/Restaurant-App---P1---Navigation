import 'package:flutter/material.dart';

class InboxScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // Màn hình Inbox đơn giản
      body: Center(child: Text("Inbox Screen", style: TextStyle(fontSize: 20))),
    );
  }
}
